class android.speech.tts.BlockingAudioTrack extends java.lang.Object
{
    private static final boolean DBG;
    private static final long MAX_PROGRESS_WAIT_MS;
    private static final long MAX_SLEEP_TIME_MS;
    private static final int MIN_AUDIO_BUFFER_SIZE;
    private static final long MIN_SLEEP_TIME_MS;
    private static final java.lang.String TAG;
    private int mAudioBufferSize;
    private final int mAudioFormat;
    private android.media.AudioTrack mAudioTrack;
    private java.lang.Object mAudioTrackLock;
    private final int mBytesPerFrame;
    private int mBytesWritten;
    private final int mChannelCount;
    private boolean mIsShortUtterance;
    private final float mPan;
    private final int mSampleRateInHz;
    private volatile boolean mStopped;
    private final int mStreamType;
    private final float mVolume;

    void <init>(int, int, int, int, float, float)
    {
        android.speech.tts.BlockingAudioTrack r0;
        int i0, i1, i2, i3, $i4, $i5, $i6, $i7;
        float f0, f1;
        java.lang.Object $r1;

        r0 := @this: android.speech.tts.BlockingAudioTrack;

        i0 := @parameter0: int;

        i1 := @parameter1: int;

        i2 := @parameter2: int;

        i3 := @parameter3: int;

        f0 := @parameter4: float;

        f1 := @parameter5: float;

        specialinvoke r0.<java.lang.Object: void <init>()>();

        r0.<android.speech.tts.BlockingAudioTrack: int mBytesWritten> = 0;

        $r1 = new java.lang.Object;

        specialinvoke $r1.<java.lang.Object: void <init>()>();

        r0.<android.speech.tts.BlockingAudioTrack: java.lang.Object mAudioTrackLock> = $r1;

        r0.<android.speech.tts.BlockingAudioTrack: int mStreamType> = i0;

        r0.<android.speech.tts.BlockingAudioTrack: int mSampleRateInHz> = i1;

        r0.<android.speech.tts.BlockingAudioTrack: int mAudioFormat> = i2;

        r0.<android.speech.tts.BlockingAudioTrack: int mChannelCount> = i3;

        r0.<android.speech.tts.BlockingAudioTrack: float mVolume> = f0;

        r0.<android.speech.tts.BlockingAudioTrack: float mPan> = f1;

        $i4 = r0.<android.speech.tts.BlockingAudioTrack: int mAudioFormat>;

        $i5 = staticinvoke <android.speech.tts.BlockingAudioTrack: int getBytesPerFrame(int)>($i4);

        $i6 = r0.<android.speech.tts.BlockingAudioTrack: int mChannelCount>;

        $i7 = $i5 * $i6;

        r0.<android.speech.tts.BlockingAudioTrack: int mBytesPerFrame> = $i7;

        r0.<android.speech.tts.BlockingAudioTrack: boolean mIsShortUtterance> = 0;

        r0.<android.speech.tts.BlockingAudioTrack: int mAudioBufferSize> = 0;

        r0.<android.speech.tts.BlockingAudioTrack: int mBytesWritten> = 0;

        r0.<android.speech.tts.BlockingAudioTrack: android.media.AudioTrack mAudioTrack> = null;

        r0.<android.speech.tts.BlockingAudioTrack: boolean mStopped> = 0;

        return;
    }

    private void blockUntilCompletion(android.media.AudioTrack)
    {
        android.speech.tts.BlockingAudioTrack r0;
        android.media.AudioTrack r1;
        int i0, i1, i3, $i5, $i6, $i7, $i8, $i9, $i10, $i12;
        long l2, l4, $l11;
        boolean $z0;
        byte $b13;
        java.lang.InterruptedException $r3;

        r0 := @this: android.speech.tts.BlockingAudioTrack;

        r1 := @parameter0: android.media.AudioTrack;

        $i6 = r0.<android.speech.tts.BlockingAudioTrack: int mBytesWritten>;

        $i5 = r0.<android.speech.tts.BlockingAudioTrack: int mBytesPerFrame>;

        i0 = $i6 / $i5;

        i1 = -1;

        l2 = 0L;

     label1:
        i3 = virtualinvoke r1.<android.media.AudioTrack: int getPlaybackHeadPosition()>();

        if i3 >= i0 goto label2;

        $i7 = virtualinvoke r1.<android.media.AudioTrack: int getPlayState()>();

        if $i7 != 3 goto label2;

        $z0 = r0.<android.speech.tts.BlockingAudioTrack: boolean mStopped>;

        if $z0 != 0 goto label2;

        $i8 = i0 - i3;

        $i10 = 1000 * $i8;

        $i9 = virtualinvoke r1.<android.media.AudioTrack: int getSampleRate()>();

        $i12 = $i10 / $i9;

        $l11 = (long) $i12;

        l4 = staticinvoke <android.speech.tts.BlockingAudioTrack: long clip(long,long,long)>($l11, 20L, 2500L);

        if i3 != i1 goto label3;

        l2 = l2 + l4;

        $b13 = l2 cmp 2500L;

        if $b13 <= 0 goto label4;

        staticinvoke <android.util.Log: int w(java.lang.String,java.lang.String)>("TTS.BlockingAudioTrack", "Waited unsuccessfully for 2500ms for AudioTrack to make progress, Aborting");

     label2:
        return;

     label3:
        l2 = 0L;

     label4:
        i1 = i3;

     label5:
        staticinvoke <java.lang.Thread: void sleep(long)>(l4);

     label6:
        goto label1;

     label7:
        $r3 := @caughtexception;

        return;

        catch java.lang.InterruptedException from label5 to label6 with label7;
    }

    private void blockUntilDone(android.media.AudioTrack)
    {
        android.speech.tts.BlockingAudioTrack r0;
        android.media.AudioTrack r1;
        int $i0;
        boolean $z0;

        r0 := @this: android.speech.tts.BlockingAudioTrack;

        r1 := @parameter0: android.media.AudioTrack;

        $i0 = r0.<android.speech.tts.BlockingAudioTrack: int mBytesWritten>;

        if $i0 > 0 goto label1;

        return;

     label1:
        $z0 = r0.<android.speech.tts.BlockingAudioTrack: boolean mIsShortUtterance>;

        if $z0 == 0 goto label2;

        specialinvoke r0.<android.speech.tts.BlockingAudioTrack: void blockUntilEstimatedCompletion()>();

        return;

     label2:
        specialinvoke r0.<android.speech.tts.BlockingAudioTrack: void blockUntilCompletion(android.media.AudioTrack)>(r1);

        return;
    }

    private void blockUntilEstimatedCompletion()
    {
        android.speech.tts.BlockingAudioTrack r0;
        long l0;
        int $i1, $i2, $i3, $i4, $i5, $i6;
        java.lang.InterruptedException $r2;

        r0 := @this: android.speech.tts.BlockingAudioTrack;

        $i2 = r0.<android.speech.tts.BlockingAudioTrack: int mBytesWritten>;

        $i1 = r0.<android.speech.tts.BlockingAudioTrack: int mBytesPerFrame>;

        $i3 = $i2 / $i1;

        $i5 = 1000 * $i3;

        $i4 = r0.<android.speech.tts.BlockingAudioTrack: int mSampleRateInHz>;

        $i6 = $i5 / $i4;

        l0 = (long) $i6;

     label1:
        staticinvoke <java.lang.Thread: void sleep(long)>(l0);

     label2:
        return;

     label3:
        $r2 := @caughtexception;

        return;

        catch java.lang.InterruptedException from label1 to label2 with label3;
    }

    private static float clip(float, float, float)
    {
        float f0, f1, f2;
        byte $b0, $b1;

        f0 := @parameter0: float;

        f1 := @parameter1: float;

        f2 := @parameter2: float;

        $b0 = f0 cmpl f2;

        if $b0 <= 0 goto label1;

        return f2;

     label1:
        $b1 = f0 cmpg f1;

        if $b1 >= 0 goto label2;

        return f1;

     label2:
        return f0;
    }

    private static final long clip(long, long, long)
    {
        long l0, l1, l2;
        byte $b3, $b4;

        l0 := @parameter0: long;

        l1 := @parameter1: long;

        l2 := @parameter2: long;

        $b3 = l0 cmp l1;

        if $b3 >= 0 goto label1;

        return l1;

     label1:
        $b4 = l0 cmp l2;

        if $b4 <= 0 goto label2;

        return l2;

     label2:
        return l0;
    }

    private android.media.AudioTrack createStreamingAudioTrack()
    {
        android.speech.tts.BlockingAudioTrack r0;
        int i0, i1, $i2, $i3, $i4, $i5, $i6, $i7, $i8, $i9;
        android.media.AudioTrack r1, $r2;
        float $f0, $f1;

        r0 := @this: android.speech.tts.BlockingAudioTrack;

        $i2 = r0.<android.speech.tts.BlockingAudioTrack: int mChannelCount>;

        i0 = staticinvoke <android.speech.tts.BlockingAudioTrack: int getChannelConfig(int)>($i2);

        $i4 = r0.<android.speech.tts.BlockingAudioTrack: int mSampleRateInHz>;

        $i3 = r0.<android.speech.tts.BlockingAudioTrack: int mAudioFormat>;

        $i5 = staticinvoke <android.media.AudioTrack: int getMinBufferSize(int,int,int)>($i4, i0, $i3);

        i1 = staticinvoke <java.lang.Math: int max(int,int)>(8192, $i5);

        $r2 = new android.media.AudioTrack;

        $i8 = r0.<android.speech.tts.BlockingAudioTrack: int mStreamType>;

        $i7 = r0.<android.speech.tts.BlockingAudioTrack: int mSampleRateInHz>;

        $i6 = r0.<android.speech.tts.BlockingAudioTrack: int mAudioFormat>;

        specialinvoke $r2.<android.media.AudioTrack: void <init>(int,int,int,int,int,int)>($i8, $i7, i0, $i6, i1, 1);

        r1 = $r2;

        $i9 = virtualinvoke r1.<android.media.AudioTrack: int getState()>();

        if $i9 == 1 goto label1;

        staticinvoke <android.util.Log: int w(java.lang.String,java.lang.String)>("TTS.BlockingAudioTrack", "Unable to create audio track.");

        virtualinvoke r1.<android.media.AudioTrack: void release()>();

        return null;

     label1:
        r0.<android.speech.tts.BlockingAudioTrack: int mAudioBufferSize> = i1;

        $f0 = r0.<android.speech.tts.BlockingAudioTrack: float mVolume>;

        $f1 = r0.<android.speech.tts.BlockingAudioTrack: float mPan>;

        staticinvoke <android.speech.tts.BlockingAudioTrack: void setupVolume(android.media.AudioTrack,float,float)>(r1, $f0, $f1);

        return r1;
    }

    private static int getBytesPerFrame(int)
    {
        int i0;
        byte b1;

        i0 := @parameter0: int;

        b1 = 2;

        if i0 != 3 goto label2;

        b1 = 1;

     label1:
        return b1;

     label2:
        if i0 == b1 goto label1;

        return -1;
    }

    static int getChannelConfig(int)
    {
        int i0;

        i0 := @parameter0: int;

        if i0 != 1 goto label1;

        return 4;

     label1:
        if i0 != 2 goto label2;

        return 12;

     label2:
        return 0;
    }

    private static void setupVolume(android.media.AudioTrack, float, float)
    {
        android.media.AudioTrack r0;
        float f0, f1, f2, f3, f4, f5, $f6, $f7;
        byte $b0, $b1;
        int $i2;

        r0 := @parameter0: android.media.AudioTrack;

        f0 := @parameter1: float;

        f1 := @parameter2: float;

        f2 = staticinvoke <android.speech.tts.BlockingAudioTrack: float clip(float,float,float)>(f0, 0.0F, 1.0F);

        f3 = staticinvoke <android.speech.tts.BlockingAudioTrack: float clip(float,float,float)>(f1, -1.0F, 1.0F);

        f4 = f2;

        f5 = f2;

        $b0 = f3 cmpl 0.0F;

        if $b0 <= 0 goto label3;

        $f7 = 1.0F - f3;

        f4 = f4 * $f7;

     label1:
        $i2 = virtualinvoke r0.<android.media.AudioTrack: int setStereoVolume(float,float)>(f4, f5);

        if $i2 == 0 goto label2;

        staticinvoke <android.util.Log: int e(java.lang.String,java.lang.String)>("TTS.BlockingAudioTrack", "Failed to set volume");

     label2:
        return;

     label3:
        $b1 = f3 cmpg 0.0F;

        if $b1 >= 0 goto label1;

        $f6 = 1.0F + f3;

        f5 = f5 * $f6;

        goto label1;
    }

    private static int writeToAudioTrack(android.media.AudioTrack, byte[])
    {
        android.media.AudioTrack r0;
        byte[] r1;
        int i0, i1, $i2, $i3, $i4;

        r0 := @parameter0: android.media.AudioTrack;

        r1 := @parameter1: byte[];

        $i2 = virtualinvoke r0.<android.media.AudioTrack: int getPlayState()>();

        if $i2 == 3 goto label1;

        virtualinvoke r0.<android.media.AudioTrack: void play()>();

     label1:
        i0 = 0;

     label2:
        $i3 = lengthof r1;

        if i0 >= $i3 goto label3;

        $i4 = lengthof r1;

        i1 = virtualinvoke r0.<android.media.AudioTrack: int write(byte[],int,int)>(r1, i0, $i4);

        if i1 > 0 goto label4;

     label3:
        return i0;

     label4:
        i0 = i0 + i1;

        goto label2;
    }

    long getAudioLengthMs(int)
    {
        android.speech.tts.BlockingAudioTrack r0;
        int i0, $i1, $i2, $i3, $i4, $i5;
        long $l6;

        r0 := @this: android.speech.tts.BlockingAudioTrack;

        i0 := @parameter0: int;

        $i1 = r0.<android.speech.tts.BlockingAudioTrack: int mBytesPerFrame>;

        $i2 = i0 / $i1;

        $i4 = 1000 * $i2;

        $i3 = r0.<android.speech.tts.BlockingAudioTrack: int mSampleRateInHz>;

        $i5 = $i4 / $i3;

        $l6 = (long) $i5;

        return $l6;
    }

    public boolean init()
    {
        android.speech.tts.BlockingAudioTrack r0;
        android.media.AudioTrack r1;
        java.lang.Object r2;
        java.lang.Throwable r3, $r4;

        r0 := @this: android.speech.tts.BlockingAudioTrack;

        r1 = specialinvoke r0.<android.speech.tts.BlockingAudioTrack: android.media.AudioTrack createStreamingAudioTrack()>();

        r2 = r0.<android.speech.tts.BlockingAudioTrack: java.lang.Object mAudioTrackLock>;

        entermonitor r2;

     label1:
        r0.<android.speech.tts.BlockingAudioTrack: android.media.AudioTrack mAudioTrack> = r1;

        exitmonitor r2;

     label2:
        if r1 != null goto label6;

        return 0;

     label3:
        $r4 := @caughtexception;

        r3 = $r4;

     label4:
        exitmonitor r2;

     label5:
        throw r3;

     label6:
        return 1;

        catch java.lang.Throwable from label1 to label2 with label3;
        catch java.lang.Throwable from label4 to label5 with label3;
    }

    public void stop()
    {
        android.speech.tts.BlockingAudioTrack r0;
        java.lang.Object r1;
        java.lang.Throwable r2, $r5;
        android.media.AudioTrack $r3, $r4;

        r0 := @this: android.speech.tts.BlockingAudioTrack;

        r1 = r0.<android.speech.tts.BlockingAudioTrack: java.lang.Object mAudioTrackLock>;

        entermonitor r1;

     label1:
        $r3 = r0.<android.speech.tts.BlockingAudioTrack: android.media.AudioTrack mAudioTrack>;

        if $r3 == null goto label2;

        $r4 = r0.<android.speech.tts.BlockingAudioTrack: android.media.AudioTrack mAudioTrack>;

        virtualinvoke $r4.<android.media.AudioTrack: void stop()>();

     label2:
        r0.<android.speech.tts.BlockingAudioTrack: boolean mStopped> = 1;

        exitmonitor r1;

     label3:
        return;

     label4:
        $r5 := @caughtexception;

        r2 = $r5;

     label5:
        exitmonitor r1;

     label6:
        throw r2;

        catch java.lang.Throwable from label1 to label2 with label4;
        catch java.lang.Throwable from label2 to label3 with label4;
        catch java.lang.Throwable from label5 to label6 with label4;
    }

    public void waitAndRelease()
    {
        android.speech.tts.BlockingAudioTrack r0;
        java.lang.Object r1, r4;
        java.lang.Throwable r2, r5, $r7, $r8;
        android.media.AudioTrack r3, $r6;
        int $i0, $i1;
        boolean $z0, $z1;

        r0 := @this: android.speech.tts.BlockingAudioTrack;

        r1 = r0.<android.speech.tts.BlockingAudioTrack: java.lang.Object mAudioTrackLock>;

        entermonitor r1;

     label01:
        r3 = r0.<android.speech.tts.BlockingAudioTrack: android.media.AudioTrack mAudioTrack>;

        exitmonitor r1;

     label02:
        if r3 != null goto label06;

        return;

     label03:
        $r8 := @caughtexception;

        r2 = $r8;

     label04:
        exitmonitor r1;

     label05:
        throw r2;

     label06:
        $i1 = r0.<android.speech.tts.BlockingAudioTrack: int mBytesWritten>;

        $i0 = r0.<android.speech.tts.BlockingAudioTrack: int mAudioBufferSize>;

        if $i1 >= $i0 goto label07;

        $z1 = r0.<android.speech.tts.BlockingAudioTrack: boolean mStopped>;

        if $z1 != 0 goto label07;

        r0.<android.speech.tts.BlockingAudioTrack: boolean mIsShortUtterance> = 1;

        virtualinvoke r3.<android.media.AudioTrack: void stop()>();

     label07:
        $z0 = r0.<android.speech.tts.BlockingAudioTrack: boolean mStopped>;

        if $z0 != 0 goto label08;

        $r6 = r0.<android.speech.tts.BlockingAudioTrack: android.media.AudioTrack mAudioTrack>;

        specialinvoke r0.<android.speech.tts.BlockingAudioTrack: void blockUntilDone(android.media.AudioTrack)>($r6);

     label08:
        r4 = r0.<android.speech.tts.BlockingAudioTrack: java.lang.Object mAudioTrackLock>;

        entermonitor r4;

     label09:
        r0.<android.speech.tts.BlockingAudioTrack: android.media.AudioTrack mAudioTrack> = null;

        exitmonitor r4;

     label10:
        virtualinvoke r3.<android.media.AudioTrack: void release()>();

        return;

     label11:
        $r7 := @caughtexception;

        r5 = $r7;

     label12:
        exitmonitor r4;

     label13:
        throw r5;

        catch java.lang.Throwable from label01 to label02 with label03;
        catch java.lang.Throwable from label04 to label05 with label03;
        catch java.lang.Throwable from label09 to label10 with label11;
        catch java.lang.Throwable from label12 to label13 with label11;
    }

    public int write(byte[])
    {
        android.speech.tts.BlockingAudioTrack r0;
        byte[] r1;
        java.lang.Object r2;
        java.lang.Throwable r3, $r5;
        android.media.AudioTrack r4;
        int i0, $i1, $i2;
        boolean $z0;

        r0 := @this: android.speech.tts.BlockingAudioTrack;

        r1 := @parameter0: byte[];

        r2 = r0.<android.speech.tts.BlockingAudioTrack: java.lang.Object mAudioTrackLock>;

        entermonitor r2;

     label1:
        r4 = r0.<android.speech.tts.BlockingAudioTrack: android.media.AudioTrack mAudioTrack>;

        exitmonitor r2;

     label2:
        if r4 == null goto label3;

        $z0 = r0.<android.speech.tts.BlockingAudioTrack: boolean mStopped>;

        if $z0 == 0 goto label7;

     label3:
        return -1;

     label4:
        $r5 := @caughtexception;

        r3 = $r5;

     label5:
        exitmonitor r2;

     label6:
        throw r3;

     label7:
        i0 = staticinvoke <android.speech.tts.BlockingAudioTrack: int writeToAudioTrack(android.media.AudioTrack,byte[])>(r4, r1);

        $i1 = r0.<android.speech.tts.BlockingAudioTrack: int mBytesWritten>;

        $i2 = i0 + $i1;

        r0.<android.speech.tts.BlockingAudioTrack: int mBytesWritten> = $i2;

        return i0;

        catch java.lang.Throwable from label1 to label2 with label4;
        catch java.lang.Throwable from label5 to label6 with label4;
    }

    public static void <clinit>()
    {
        <android.speech.tts.BlockingAudioTrack: java.lang.String TAG> = "TTS.BlockingAudioTrack";

        <android.speech.tts.BlockingAudioTrack: long MIN_SLEEP_TIME_MS> = 20L;

        <android.speech.tts.BlockingAudioTrack: int MIN_AUDIO_BUFFER_SIZE> = 8192;

        <android.speech.tts.BlockingAudioTrack: long MAX_SLEEP_TIME_MS> = 2500L;

        <android.speech.tts.BlockingAudioTrack: long MAX_PROGRESS_WAIT_MS> = 2500L;

        <android.speech.tts.BlockingAudioTrack: boolean DBG> = 0;

        return;
    }
}

class android.widget.Scroller$ViscousFluidInterpolator extends java.lang.Object implements android.view.animation.Interpolator
{
    private static final float VISCOUS_FLUID_SCALE;
    private static final float VISCOUS_FLUID_NORMALIZE;
    private static final float VISCOUS_FLUID_OFFSET;

    void <init>()
    {
        android.widget.Scroller$ViscousFluidInterpolator r0;

        r0 := @this: android.widget.Scroller$ViscousFluidInterpolator;

        specialinvoke r0.<java.lang.Object: void <init>()>();

        return;
    }

    private static float viscousFluid(float)
    {
        float f0, f1, $f2, $f3, $f4, $f5, $f6, $f7, $f8, f9, f10, f11;
        byte $b0;
        double $d0, $d1, $d2, $d3;

        f0 := @parameter0: float;

        f9 = f0 * 8.0F;

        $b0 = f9 cmpg 1.0F;

        if $b0 >= 0 goto label1;

        $f6 = neg f9;

        $d2 = (double) $f6;

        $d3 = staticinvoke <java.lang.Math: double exp(double)>($d2);

        $f7 = (float) $d3;

        $f8 = 1.0F - $f7;

        f10 = f9 - $f8;

        goto label2;

     label1:
        f1 = 0.36787945F;

        $f2 = 1.0F - f9;

        $d0 = (double) $f2;

        $d1 = staticinvoke <java.lang.Math: double exp(double)>($d0);

        $f3 = (float) $d1;

        f11 = 1.0F - $f3;

        $f4 = 1.0F - f1;

        $f5 = f11 * $f4;

        f10 = f1 + $f5;

     label2:
        return f10;
    }

    public float getInterpolation(float)
    {
        android.widget.Scroller$ViscousFluidInterpolator r0;
        float f0, f1, $f2, $f3, $f4, $f5;
        byte $b0;

        r0 := @this: android.widget.Scroller$ViscousFluidInterpolator;

        f0 := @parameter0: float;

        $f2 = <android.widget.Scroller$ViscousFluidInterpolator: float VISCOUS_FLUID_NORMALIZE>;

        $f3 = staticinvoke <android.widget.Scroller$ViscousFluidInterpolator: float viscousFluid(float)>(f0);

        f1 = $f2 * $f3;

        $b0 = f1 cmpl 0.0F;

        if $b0 <= 0 goto label1;

        $f4 = <android.widget.Scroller$ViscousFluidInterpolator: float VISCOUS_FLUID_OFFSET>;

        $f5 = f1 + $f4;

        return $f5;

     label1:
        return f1;
    }

    static void <clinit>()
    {
        float $f0, $f1, $f2, $f3, $f4, $f5;

        <android.widget.Scroller$ViscousFluidInterpolator: float VISCOUS_FLUID_SCALE> = 8.0F;

        $f0 = staticinvoke <android.widget.Scroller$ViscousFluidInterpolator: float viscousFluid(float)>(1.0F);

        $f1 = 1.0F / $f0;

        <android.widget.Scroller$ViscousFluidInterpolator: float VISCOUS_FLUID_NORMALIZE> = $f1;

        $f2 = <android.widget.Scroller$ViscousFluidInterpolator: float VISCOUS_FLUID_NORMALIZE>;

        $f3 = staticinvoke <android.widget.Scroller$ViscousFluidInterpolator: float viscousFluid(float)>(1.0F);

        $f4 = $f2 * $f3;

        $f5 = 1.0F - $f4;

        <android.widget.Scroller$ViscousFluidInterpolator: float VISCOUS_FLUID_OFFSET> = $f5;

        return;
    }
}
